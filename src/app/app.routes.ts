import { Routes } from '@angular/router';
import { ChargeComponent } from './features/only-charge/pages/charge/charge.component';

export const routes: Routes = [
  {
    path: '',
    component: ChargeComponent,
    title: 'Only Charge',
  },
];
